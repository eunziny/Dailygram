<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kitri.daily.board.BoardMapper">

	<resultMap id="BoardResult" type="com.kitri.daily.board.Board">
		<result column="board_seq" property="board_seq" />
		<result column="writer" property="writer" />
		<result column="content" property="content" />
		<result column="posted" property="posted" />
		<result column="img1" property="img1" />
		<result column="img2" property="img2" />
		<result column="img3" property="img3" />
		<result column="img4" property="img4" />
		<result column="img5" property="img5" />
		<result column="public_yn" property="public_yn" />
	</resultMap>

	<select id="select" resultMap="BoardResult" 
		parameterType="String">
		select * from board where writer=#{writer}, board_seq = #{board_seq}
	</select>
	
	<insert id="insert" parameterType="com.kitri.daily.board.Board">
		insert into board values
		(#{board_seq}, #{writer}, #{content}, #{posted}, #{img1}, 
		#{img2}, #{img3}, #{img4}, #{img5}, #{public_yn})
	</insert>
	
	<update id="update" parameterType="com.kitri.daily.board.Board">
		update board
		set
		content=#{content}, img1=#{img1}, img2=#{img2}, img3=#{img3},  
		img4=#{img4}, img5=#{img5}, public_yn=#{public_yn}
		where writer=#{writer}, board_seq=#{board_seq}
	</update>
	
	<delete id="delete" parameterType="String">
		delete board
		where writer=#{writer}, board_seq=#{board_seq}
	</delete>
	
	<select id="selectAll" resultMap="BoardResult" 
		parameterType="String">
		select * from board where writer=#{writer}
	</select>
	
</mapper>